# Задание 1
```
print(f'Привет, {input('Имя: ')}! Через год тебе будет {int(input('Возраст: '))+1}.')
```
![01_greeting](images/lab01/01_greeting.png)
### Запрашиваю у пользователя имя и возраст через input, вложенные в f строку. Таким образом выполняю всё за 1 строчку.

# Задание 2
```
sum = float(input('a: ').replace(',', '.')) + float(input('b: ').replace(',', '.'))
avg = sum/2
print(f"{sum=:.2f}; {avg=:.2f}")
```
 ![02_sum_avg](images/lab01/02_sum_avg.png)
 ### Через input запрашиваю у пользователя числа a и b. На случай неразумности пользователя заменяю ',' на '.', чтобы не выдало ошибку (python читает float именно с точкой). Вывожу результаты с точностью до 2 знака после запятой через f строчку.

# Задание 3
```
price = float(input('Цена: ').replace(',', '.'))
discount = float(input('Скидка: ').replace(',', '.'))
vat = float(input('НДС: ').replace(',', '.'))

base = price * (1 - discount/100)
vat_amount = base * (vat/100)
total = base + vat_amount

print(f'{'База после скидки:':<20} {base:.2f}')
print(f'{'НДС:':<20} {vat_amount:.2f}')
print(f'{'Итого к оплате:':<20} {total:.2f}')
```
![03_discount_vat](images/lab01/03_discount_vat.png)
### Cчитываю ввод, аналогично 2 заданию исправляю ',' на '.', далее через приложенные в задании формулы произвожу рассчёты. Вывожу через f строчки, делая отступы, чтобы текст получился ровным по правому краю, как в примере к заданию.

# Задание 4
```
minutes = int(input("Минуты: "))

print(f"{minutes//60}:{minutes%60:02d}")
```
![04_minutes_to_hhmm](images/lab01/04_minutes_to_hhmm.png)
### Cчитываю ввод в минутах. Для оптимизации кода произвожу рассчёт сразу в выводе, при помощи f строчки: для получения количества часов нацело делю количество минут на 60. Для получения количества минут беру остаток от деления изначального количества минут на 60, оставляя ровно 2 символа после часов с помощью метода, который был приведён в тексте задания.

# Задание 5
```
fio = input("ФИО: ")
print(f"Инициалы: {"".join([i[0].upper() for i in fio.split()])}.")
print(f"Длина (символов): {len(" ".join(fio.split()))}")
```
![05_initials_and_len](images/lab01/05_initials_and_len.png)
### Считываю ввод ФИО пользователя в 1 строчку. Для оптимизации произвожу все операции с введённым ФИО сразу же в выводе, через f строки и генераторы. Зная, что заглавные буквы всегда идут впереди, я делю ФИО по пробелам, а затем беру из полученных элементов первый (нулевой) символ. Таким образом, через склеивание получаю инициалы. Для получения длины ФИО сначала сплитую (чтобы избавиться от лишних пробелов), затем склеиваю в одну строчку ровно с 1 пробелом между частями ФИО. Использую len для получения количества символов.
